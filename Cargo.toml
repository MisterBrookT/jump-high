[package]
name = "jump-high"
version = "0.2.0"
edition = "2021"
description = "A Jump King-style terminal platformer — hold SPACE to charge, release to jump, don't fall!"
license = "MIT"

[dependencies]
ratatui = "=0.29.0"
crossterm = "=0.28.1"
